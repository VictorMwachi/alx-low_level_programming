#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * _strlen-returns the length of a string
 * @s: string s
 * Return: returns string length
 */
int _strlen(char *s)
{
	return (strlen(s));
}
