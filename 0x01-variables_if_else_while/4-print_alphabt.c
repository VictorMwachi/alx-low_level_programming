#include <stdio.h>
/**
 * main-entry point
 *
 * Return:always o (success)
 */
int main(void)
{char ch = a;
while (ch <= 'z')
{if (ch != 'q' && ch != 'e')
{putchar(ch); }
}
putchar('\n');
return 0;
}
