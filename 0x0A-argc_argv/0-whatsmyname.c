#include <stdio.h>
/**
 * main - program entry point
 * @argc: size of agg
 * @argv: array of argurments
 * Return: always (0) success
 */
int main(int argc, char *argv[])
{
	printf("%s\n",argv[0]);
	return (0);
}
